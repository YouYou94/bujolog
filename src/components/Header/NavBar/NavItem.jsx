import * as Styled from '../styled.jsx';

export const Home = () => {
  return <Styled.HeaderNavItem>home</Styled.HeaderNavItem>;
};

export const Mybujo = () => {
  return <Styled.HeaderNavItem>mybujo</Styled.HeaderNavItem>;
};

export const Bujotalk = () => {
  return <Styled.HeaderNavItem>bujotalk</Styled.HeaderNavItem>;
};

export const Logout = () => {
  return <Styled.HeaderNavItem>logout</Styled.HeaderNavItem>;
};
