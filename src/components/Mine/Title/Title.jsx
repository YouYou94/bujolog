import * as Styled from './TitleStyled.jsx';

const Title = () => {
  return (
    <Styled.MineTitleBox>
      <Styled.MineTitle>Mine</Styled.MineTitle>
    </Styled.MineTitleBox>
  );
};

export default Title;
