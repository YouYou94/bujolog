import * as Styled from './SectionStyled.jsx';

const Task = () => {
  return (
    <Styled.SectionContainer>
      <Styled.SectionTitle>Task</Styled.SectionTitle>
    </Styled.SectionContainer>
  );
};

export default Task;
