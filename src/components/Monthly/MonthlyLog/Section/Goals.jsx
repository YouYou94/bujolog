import * as Styled from './SectionStyled.jsx';

const Goals = () => {
  return (
    <Styled.SectionContainer>
      <Styled.SectionTitle>Goals</Styled.SectionTitle>
    </Styled.SectionContainer>
  );
};

export default Goals;
